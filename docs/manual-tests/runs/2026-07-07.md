# Mobile pre-release checklist

| | |
|--|--|
| Date | 2026-07-07 |
| Tester | Pierre |
| Branch / commit | staging / 25cec14 |
| Target URL | https://staging-confessio.pcdhebrail.fr |

Walk through both runs on the listed device. Each run takes ~10 minutes.
Mark each step `✅`, `❌` (with a one-line note), or `N/A`.

---

## Run A — iPhone / iOS Safari

**Reference device:** iPhone 14 or newer, iOS 17+, Safari (private window OK).
**Why:** WebKit + iOS gesture nav + safe-area insets are the most likely
source of finicky bugs.

`N/A` — no iPhone available for this run.

### Cold load & layout
- [ ] `N/A` Open the home URL in a fresh tab. Map tiles render within ~3s, no white flash.
- [ ] `N/A` No content is hidden behind the notch or the home indicator.
- [ ] `N/A` Address bar is visible at first; scrolling the bottom sheet up causes it to collapse without the layout jumping (no `100vh` / `100dvh` pop).
- [ ] `N/A` Rotate to landscape and back to portrait — layout adapts cleanly, no overlap.

### Map & search
- [ ] `N/A` Pinch-zoom and two-finger pan work; the page itself doesn't pan.
- [ ] `N/A` Tap the search input. Virtual keyboard opens; the search input stays visible (not hidden under the keyboard).
- [ ] `N/A` Type a city (e.g. "Lyon"). Suggestions appear within ~500ms of stopping typing.
- [ ] `N/A` Tap a suggestion → map pans / zooms to the result.
- [ ] `N/A` Dismiss keyboard (tap outside or "Done"). Bottom sheet position is unaffected.

### Bottom sheet (the finicky one)
- [ ] `N/A` Tap a church marker → bottom sheet opens at the **middle** snap point.
- [ ] `N/A` Drag the handle up to the **top** snap point. The content area inside the sheet scrolls (not the page).
- [ ] `N/A` At the top snap, scroll the content all the way to the bottom — overscroll bounce is contained inside the sheet, doesn't drag the map.
- [ ] `N/A` At the top snap, scroll back up; the sheet doesn't snap down accidentally.
- [ ] `N/A` Drag the handle down past the middle snap → sheet collapses.
- [ ] `N/A` Tap a different marker while sheet is open — content updates, snap state stays at middle.
- [ ] `N/A` Close the sheet (drag down to bottom / tap outside / X if any). No marker stays "selected" visually.

### Navigation modal
- [ ] `N/A` Open the menu (burger / nav button). Modal overlays the map.
- [ ] `N/A` Tap outside the modal — closes.
- [ ] `N/A` Tap "Nous contacter" — leaves to confessio.fr/contact. Hit back: returns to the map at the same state (zoom, marker selection).

### Deep links / SEO routes
- [ ] `N/A` Open `/diocese/paris` directly. Page resolves to the map zoomed/bounded on Paris within ~2s. Browser back works (does not loop).
- [ ] `N/A` Open a `/church/<uuid>` URL directly. Page does not crash. (Currently bare; just verify no error screen.)
- [ ] `N/A` Hit a non-existent slug, e.g. `/diocese/zzz-not-real`. App shows a 404, not a server error.

### Edge cases
- [ ] `N/A` Toggle airplane mode briefly while on the map. Reconnect: app recovers (markers reload or state stays).
- [ ] `N/A` Background the app for 1 minute, return: state is preserved, no white screen.

---

## Run B — Android / Chrome

**Reference device:** Pixel 7 / 8 or Samsung Galaxy S22+, current Chrome stable.
**Why:** Blink engine, Android virtual keyboard sizing, edge-swipe-back gesture, different scroll-chaining behaviour against the map.

### Cold load & layout
- [x] Open the home URL fresh. Map tiles render within ~3s.
- [ ] On a device with a navigation bar (3-button), the bottom sheet bottom snap doesn't sit *under* the nav bar.
- [x] On a gesture-nav device, the bottom of the sheet leaves room for the gesture indicator.
- [x] Rotate landscape → portrait. Layout adapts.

### Map & search
- [x] Pinch-zoom and two-finger pan work; the page itself doesn't scroll.
- [x] Tap search input. Android keyboard opens; **the bottom sheet doesn't get pushed off-screen** (a real Android-specific bug pattern).
- [x] Type "Lyon" → suggestions debounced.
- [x] Tap a suggestion → map pans.
- [x] Dismiss keyboard with the back button (system gesture). Sheet returns to its prior snap, no flicker.

### Bottom sheet
- [x] Tap marker → sheet opens at middle snap.
- [x] Drag up to top snap; inner content scrolls; outer page does not scroll under the sheet.
- [x] At top snap, do a fast flick scroll inside the content. No "double scroll" — the map below should not move.
- [x] Drag handle down → collapses cleanly.
- [x] Tap a different marker → content updates, snap stays at middle.

### Navigation modal & system back
- [x] Open menu modal. Press the **system back gesture** (edge swipe) — modal closes (or app navigates back to the map, depending on intent). It must not leave the user stranded on a blank screen.
- [x] In the modal, tap "Nous contacter" → leaves to confessio.fr. System back returns to the map.

### Deep links / SEO routes
- [x] Open `/diocese/paris` directly. Resolves to the map. Hit system back: behaviour is sane (does not infinite-redirect).
- [x] Open a `/church/<uuid>` URL directly. Does not crash.
- [x] Hit a non-existent slug, e.g. `/diocese/zzz-not-real`. 404 page, not a server error.

### Edge cases
- [x] Toggle airplane mode briefly. Reconnect: app recovers.
- [x] Pull-to-refresh from the top of the page — confirm Chrome's PTR doesn't fight with the sheet (either the sheet absorbs the gesture or PTR works cleanly).
- [x] Background app for 1 minute, return: state preserved.

---

## Reporting issues

For every `❌`:
1. Note OS / browser version exactly (Settings → About).
2. Screen-record if possible (Android: built-in recorder).
3. File a Linear ticket with the recording, the reproduction steps from this checklist, and a link to the run file in `runs/`.
